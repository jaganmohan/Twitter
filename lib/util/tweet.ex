defmodule Util.Tweet do
    @moduledoc """
    Struct defining tweet contents.
    """
    defstruct id: nil, hashtags: [], mentions: [], msg: "", origin: nil

    def extractTags(tweet) do
        
    end

    def extractMentions(tweet) do
        
    end

end
